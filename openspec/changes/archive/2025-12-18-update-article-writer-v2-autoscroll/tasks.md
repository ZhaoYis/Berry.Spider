## 1. 实施
- [x] 1.1 分析 ArticleWriterAgentV2 当前 AI 回答展示方式（`TextBox` + `AiResponseText` 绑定）与 Avalonia 支持的自动滚动方案（如 `ScrollViewer`、附加属性或行为）。
- [x] 1.2 设计自动滚动策略：在 `AiResponseText` 文本追加时，默认滚动到底部，但当用户手动向上滚动时不强制抢占滚动位置。
- [x] 1.3 在 `ArticleWriterAgentV2.axaml` 及必要的代码隐藏/ViewModel 中实现自动滚动逻辑，保持现有布局与绑定不变。
- [x] 1.4 在桌面应用中手动验证：
  - 输入较长提示词触发 AI 持续生成内容；
  - 确认文本框会随内容追加自动滚动到底部；
  - 确认用户向上滚动查看历史时不会被强制拉回底部。
- [x] 1.5 清理与代码审查：确保新逻辑简单可读、无冗余调试代码，并与现有 UI 代码风格保持一致。

## 2. 验证
- [x] 2.1 运行桌面应用，多次验证 AI 回答文本框在不同长度、不同生成速度下的滚动行为。
- [x] 2.2 如有自动化 UI 测试基础，评估是否可以添加简单的行为测试（可选）。
- [x] 2.3 确认无异常日志、无 UI 卡顿或明显性能问题。
